spring:
  application:
    name: operation-service
  data:
    mongodb:
      uri: mongodb://db_operation:27017/db_operation
server:
  port: 8083
message:
  path-operation: /api/v1/operations
  path-operationType: /api/v1/operationTypes
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://ms-discovery:8889/eureka/