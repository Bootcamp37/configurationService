spring:
  application:
    name: query-balance-service
server:
  port: 8086
message:
  path-balance: /api/v1/balance
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://ms-discovery:8889/eureka/