spring:
  application:
    name: payment-service
  data:
    mongodb:
      uri: mongodb://db_payment:27017/db_payment
server:
  port: 8085
message:
  path-payment: /api/v1/payments
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://ms-discovery:8889/eureka/